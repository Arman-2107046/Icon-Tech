import { Container, Section } from "@/src/storefront/components/layout";

/** Placeholder until the homepage sections land (items 59–60). */
export default function HomePage() {
  return (
    <Section space="xl">
      <Container>
        <h1 className="display display-4xl max-w-3xl">Well-made things for the desk, the bag, and the pocket.</h1>
      </Container>
    </Section>
  );
}
