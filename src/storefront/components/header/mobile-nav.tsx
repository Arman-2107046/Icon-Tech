"use client";

import type { StorefrontMenu } from "@/src/modules/content/types";

/** Placeholder until item 56 builds the drawer. */
export function MobileNav(props: { items: StorefrontMenu["items"]; storeName: string }) {
  void props;
  return null;
}
